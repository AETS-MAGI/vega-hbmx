# Glossary

## 用語
- `run_id`: 実験実行ごとの識別子。
- `backend`: 実行系の区別。例: ROCm/HIP, Vulkan。
- `num_gpu`: 実験系では GPU 枚数ではなく offload 層数として扱う。
- `matched conditions`: model, prompt, epoch, backend 以外の主要条件を揃えた比較条件。

## 書き方メモ
- 略語は最初に正式名称を書く。
- 解釈と観察は分けて書く。
