# Environment

## このノートで固定的に参照したい情報
- project: `vega-investigate`
- target GPU: AMD Radeon RX Vega 56 (`gfx900`)
- 主な比較対象 backend: ROCm/HIP, Vulkan

## 記録方針
日々変わる値は日報に書き、比較の前提になる共通条件だけここに残す。

### 例
- OS / kernel
- Ollama version
- ROCm version
- 主要モデル
- 比較用 endpoint
