最優先
1. 同一条件での再現確認をもう1セット
目的:

「たまたま1回通った/落ちた」を避ける
提案:

ROCm と Vulkan で同じ
model
prompt
NUM_PREDICT=512
num_gpu=0,1,2,-1
これをもう1セットだけ回す
効く点:

「再現性ありますか？」への即答材料になります。
2. num_gpu=0 の Vulkan 安定性確認
目的:

Vulkan を「全面的にダメ」と言っていないことを補強
提案:

Vulkan だけ num_gpu=0 で 3〜5 epoch
連続成功率を確認
効く点:

“Vulkan is conditionally usable, but unstable on offload paths in this setup” が言いやすくなります。
次点
3. モデルを1つだけ追加
目的:

qwen3.5:2b 固有かどうかを見る
提案:

軽いモデルを1つだけ追加
条件は num_gpu=0 と num_gpu=1 だけで十分
効く点:

「モデル依存の可能性」への備えになります。
全条件総当たりは不要です。
4. ROCm 側の override 有無の確認
目的:

HSA_OVERRIDE_GFX_VERSION=9.0.0 の寄与を明示する
提案:

可能なら num_gpu=0 だけでも
override あり
override なし
を比較
効く点:

「ROCm がそのまま動いた」のではなく、override-assisted であることを整理できます。
失敗しても価値があります。
時間があれば
5. NUM_PREDICT 短縮条件での crash phase 確認
目的:

落ちる条件の位相をもう少し詰める
提案:

Vulkan num_gpu=1
NUM_PREDICT=64 か 128
journal だけ確認
効く点:

「長時間実行で壊れる」のか「offload compute に入った瞬間に壊れる」のかが少し見えます。
6. prompt 固定で出力差ではなく実行成否だけを見る
目的:

内容差ではなく安定性評価に集中
提案:

短い固定 prompt 1本
判定は
HTTP status
runner crash
eval_count
elapsed
効く点:

発表時の議論がぶれません。
発表直前向けの実務的おすすめ
もし時間が少ないなら、この3本だけで十分です。

ROCm/Vulkan 同一条件の再実行 1セット
Vulkan num_gpu=0 の複数回確認
別モデルを1つだけ 0/1 で試す
これで、

再現性
条件依存性
モデル依存性
の3点を最低限押さえられます。
スライドに反映しやすい一文
追試後に入れるなら、こういう文が安全です。

The observed behavior was reproduced in repeated matched runs.
In the tested setup, Vulkan remained stable at num_gpu=0 but failed on offloaded-layer settings.
A small additional model check suggested that the issue is not trivially explained by a single prompt/output artifact.
必要なら次に、「今夜回すならこのコマンド順」 まで絞って提案できます。

